#include<stdio.h>
#include<math.h>
void main()
{
    int i=0,n=0;
    printf("Enter the value of n\n");
    scanf("%d",&n);
    fflush(stdin);
    for(i=2;i<=n;i++)
    {
        printf("%d",pow(i,i));
    }
    getch();
    }
