class FirstThread1
{
     public static void main(String args[])
     {
         Thread t=Thread.currentThread();
         System.out.println("First thread after thread creation "+t.getName());
     }
}
